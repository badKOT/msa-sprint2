apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: booking-service-vs
spec:
  hosts:
    - booking-service
    - localhost
  gateways:
    - booking-service-gw
  http:
    - route:
        - destination:
            host: booking-service
            subset: v1
          weight: 90
        - destination:
            host: booking-service
            subset: v2
          weight: 10
      retries:
        attempts: 1
        retryOn: 5xx
        perTryTimeout: 2s
